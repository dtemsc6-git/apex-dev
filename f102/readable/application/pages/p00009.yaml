---
# ====== Page: Dependencias Sitio ============================
id: 9
identification: 
  name: Dependencias Sitio
  alias: DEPENDENCIAS-SITIO
  title: Dependencias Sitio

appearance: 
  page-mode: Modal Dialog
  dialog-template: Theme Default
  template-options: 
  - '#DEFAULT#'

dialog: 
  chained: true
  resizable: true

navigation-menu: 
  override-user-interface-level: false

navigation: 
  cursor-focus: Do not focus cursor
  warn-on-unsaved-changes: true

security: 
  authentication: Page Requires Authentication
  deep-linking: Application Default
  page-access-protection: Arguments Must Have Checksum
  form-auto-complete: Off
  browser-cache: Application Default

session-management: 
  rejoin-sessions: Application Default

advanced: 
  enable-duplicate-page-submissions: Yes - Enable page to be re-posted
  reload-on-submit: Only for Success

server-cache: 
  caching: Disabled

regions: 
- # ====== Region: Dependencias ================================
  id: 9346709752692581
  identification: 
    name: Dependencias
    type: Tree

  source: 
    location: Local Database
    type: SQL Query
    sql-query: |
      with data as (
      select ('SC') as link_type, 'S-'||to_char(sitio_id) as PARENT_ID, 'SC-'||to_char(c.id) as ID, c.siglas ||' - '||c.nombre as name, c.id as sub_id from sgt_salas c
      union all
      select ('R') as link_type,'SC-'||to_char(sala_id) as PARENT_ID, 'R-'||to_char(d.id) as ID, d.nombre ||' - '||d.detalle as name, d.id as sub_id from sgt_racks d  
      union all 
      select ('E') as link_type,  'R-'||to_char(rack_id) as parent_id , 'E-'||to_char(e.id) as ID, 'U: '||e.u||', '||e.nombre as name, e.id as sub_id from sgt_equipos e
      
      
      )
      select case when connect_by_isleaf = 1 then 0
                  when LEVEL = 1             then 1
                  else                           -1
             end as status, 
             LEVEL,
             name as title, 
             null as icon, 
             "ID" as value, 
             null as tooltip, 
             null as link 
      from data 
      
      start with "PARENT_ID" = 'S-'||:P9_ID
      connect by prior "ID" = "PARENT_ID"
      order siblings by name
    page-items-to-submit: 
    - P9_ID

  layout: 
    sequence: 20
    parent-region: No Parent
    slot: BODY
    start-new-layout: false
    start-new-row: true
    column: Automatic
    new-column: true
    column-span: Automatic

  appearance: 
    template: Standard
    template-options: 
    - '#DEFAULT#'
    - t-Region--noPadding
    - t-Region--removeHeader js-removeLandmark
    - t-Region--noBorder
    - t-Region--scrollBody
    render-components: Above Content

  accessibility: 
    use-landmark: true
    landmark-type: Template Default

  advanced: 
    region-display-selector: false
    exclude-title-from-translation: false

  server-cache: 
    caching: Disabled

  customization: 
    customizable: Not Customizable By End Users

  attributes: 
    settings: 
      node-label-column: TITLE
      node-value-column: VALUE
      hierarchy: Not Computed
      node-status-column: STATUS
      hierarchy-level-column: LEVEL
      tooltip: None
      activate-node-link-with: Single Click
      link-column: LINK
      default-icon-css-class: icon-tree-folder
      icon-type-css-class: a-Icon

    performance: 
      lazy-loading: false

- # ====== Region: New =========================================
  id: 1831411813863327
  identification: 
    name: New
    type: Static Content

  source: 
    html-code: No se encontraron datos

  layout: 
    sequence: 30
    parent-region: No Parent
    slot: BODY
    start-new-layout: false
    start-new-row: true
    column: Automatic
    new-column: true
    column-span: Automatic

  appearance: 
    template: Alert
    template-options: 
    - '#DEFAULT#'
    - t-Alert--horizontal
    - t-Alert--defaultIcons
    - t-Alert--warning
    - t-Alert--removeHeading js-removeLandmark
    render-components: Above Content

  accessibility: 
    use-landmark: true
    landmark-type: Template Default

  advanced: 
    region-display-selector: false
    exclude-title-from-translation: false

  server-side-condition: 
    type: No Rows returned
    sql-query: |
      select id from sgt_salas
      where sitio_id = :P9_ID

  customization: 
    customizable: Not Customizable By End Users

  attributes: 
    settings: 
      output-as: HTML
      expand-shortcuts: false

page-items: 
- # ====== Page Item: P9_ID ====================================
  id: 9346820997692582
  identification: 
    name: P9_ID
    type: Hidden

  settings: 
    value-protected: true

  layout: 
    sequence: 10
    region: No Parent
    slot: BODY

  advanced: 
    warn-on-unsaved-changes: Page Default

  source: 
    type: Null
    used: Only when current value in session state is null

  session-state: 
    data-type: VARCHAR2
    storage: Per Session (Persistent)

  security: 
    session-state-protection: Unrestricted
    store-value-encrypted-in-session-state: true
    restricted-characters: All characters can be saved.

